import { WheatherIconDirective } from './wheather-icon.directive';

describe('WheatherIconDirective', () => {
  it('should create an instance', () => {
    const directive = new WheatherIconDirective();
    expect(directive).toBeTruthy();
  });
});
